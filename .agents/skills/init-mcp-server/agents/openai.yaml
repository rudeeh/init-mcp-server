interface:
  display_name: "Init Mcp Server"
  short_description: "Repo-specific patterns and workflows for init-mcp-server"
  default_prompt: "Use the init-mcp-server repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true